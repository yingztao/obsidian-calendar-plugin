# obsidian-calendar-plugin

这是一个用于 [Obsidian](https://obsidian.md/) 的插件，提供简洁的日历视图，用于可视化并在你的日记（Daily Notes）之间快速导航。

![screenshot-full](https://raw.githubusercontent.com/liamcain/obsidian-calendar-plugin/master/images/screenshot-full.png)

## 用法

在设置中启用插件后，你应该会在右侧边栏看到日历视图。

插件会读取你的 Daily Note 设置，以确定日期格式、日记模板位置，以及新建日记时使用的目标文件夹。

## 功能

- 跳转到任意**日记（daily note）**。
- 为尚未存在笔记的日期创建新的日记。（当你需要补写历史笔记，或提前规划未来笔记时很有帮助！创建时会使用你当前的**日记模板**。）
- 可视化你的写作量。每一天都会显示一个计量点阵，用于近似表示当天写了多少内容。
- 使用**周记（Weekly notes）**作为额外的组织层级。它和日记类似，但有单独的自定义选项。

## 设置

- **Start week on [default: locale]**：配置日历视图每周以周日或周一开始。选择 `locale` 时，将按你当前语言环境的默认设置（`Settings > About > Language`）。
- **Words per Dot [default: 250]**：从 1.3 版本开始，圆点表示文件字数。默认每个圆点代表 250 词，你可以改成任意值。设为 `0` 可完全禁用字数圆点。**注意：**最多显示 5 个圆点，避免视图过大。
- **Confirm before creating new note [default: on]**：如果你不希望在创建新日记前弹出确认框，可以关闭此选项。
- **Show Week Number [default: off]**：启用后会在日历中新增一列显示 [周序号](https://en.wikipedia.org/wiki/Week#Week_numbering)。点击这些单元格可打开对应的**周记**。

## 自定义

你可以在 `obsidian.css` 中覆盖以下 CSS 变量。

```css
/* obsidian-calendar-plugin */
/* https://github.com/liamcain/obsidian-calendar-plugin */

#calendar-container {
  --color-background-heading: transparent;
  --color-background-day: transparent;
  --color-background-weeknum: transparent;
  --color-background-weekend: transparent;

  --color-dot: var(--text-muted);
  --color-arrow: var(--text-muted);
  --color-button: var(--text-muted);

  --color-text-title: var(--text-normal);
  --color-text-heading: var(--text-muted);
  --color-text-day: var(--text-normal);
  --color-text-today: var(--interactive-accent);
  --color-text-weeknum: var(--text-muted);
}
```

除了 CSS 变量外，还有一些 class 可以覆盖以做更细粒度的自定义。比如如果你觉得标题太亮，可以这样覆盖：

```css
#calendar-container .year {
  color: var(--text-normal);
}
```

> **注意：**覆盖 class 时务必加上 `#calendar-container` 前缀，避免对 Obsidian 中其他元素产生意外影响。

### 给主题作者的提醒

如果你对日历使用 “Inspect Element”，会看到一些不太易读的 CSS 类名，例如 `.task.svelte-1lgyrog.svelte-1lgyrog`。这是为什么？以 `svelte-` 开头的类是自动生成的，用来避免日历样式影响应用中的其他元素。也就是说：**请忽略它们！**这些类名在不同版本之间很可能变化，你的覆盖样式会失效。请只针对可读部分的类名。例如要覆盖 `task.svelte-1lgyrog.svelte-1lgyrog`，应使用 `#calendar-container .task { ... }`。

## 兼容性

`obsidian-calendar-plugin` 当前要求 Obsidian v0.9.11 或更高版本。

## 安装

你可以在 Obsidian 的 Community Plugins（社区插件）中安装。搜索 `Calendar` 即可。

## 常见问题（FAQ）

### 这些圆点是什么意思？

每个实心圆点代表你当天日记中的 250 个词。比如 4 个点表示你当天写了大约一千词。如果想调整阈值，可以在日历设置中修改 “Words Per Dot”。

空心圆点则表示当天存在未完成任务。(**注意：**无论当天有多少未完成任务，只会显示 1 个空心圆点。)

### 如何修改 Calendar 的样式？

默认情况下，日历会尽量与当前主题保持一致。但如果你想进一步自定义，也可以做到。在你的 `obsidian.css`（位于 vault 内）中可以自由配置样式。

### 我可以在日历里显示周数吗？

在设置里启用 “Show Week Numbers” 后，日历会增加“周数”列。点击周数即可打开对应“周记”。

### 不禁用插件的情况下，如何隐藏日历？

和其他边栏视图（如 Backlinks、Outline）一样，你可以右键视图图标并关闭该视图。

![how-to-close](./images/how-to-close.png)

### 我不小心关掉了日历，怎么重新打开？

如果你关闭了日历组件（右键面板导航并点击 close），可以随时在命令面板重新打开。搜索 `Calendar: Open view` 即可。

![how-to-reopen](./images/how-to-reopen.png)

### 如何让日历从周一开始？

在设置中切换为 “Start week on Monday”。

### 如何在周记文件名里包含“不参与格式化”的文字？

如果你希望周记格式中包含普通文字（例如 “Week 21 of Year 2020”），可以用 `[]` 将文字包起来。这会告诉 [moment](https://momentjs.com/docs/#/displaying/format/) 忽略这些文字。以上例为例，可将格式设为 `[Week] ww [of Year] gggg`。

### 我不想显示周数，但仍想使用周记，可以吗？

可以。你可以在命令面板中搜索 `Calendar: Open weekly Note` 打开当前周的周记。

若要配置 `format`、`folder`、`template`，需要临时在设置中开启 “Show weekly numbers”；配置完成后再关闭也没问题，设置会保留。

## 使用技巧（Protips）

### 在周记中嵌入整周内容

将以下片段加入周记模板，就可以一键在单页中查看整周内容。

```md
## Week at a Glance

![[{{sunday:gggg-MM-DD}}]]
![[{{monday:gggg-MM-DD}}]]
![[{{tuesday:gggg-MM-DD}}]]
![[{{wednesday:gggg-MM-DD}}]]
![[{{thursday:gggg-MM-DD}}]]
![[{{friday:gggg-MM-DD}}]]
![[{{saturday:gggg-MM-DD}}]]
```

### 悬停预览（Hover Preview）

和 Obsidian 的图谱与内部链接类似，日历支持页面预览。按住键盘上的 `Ctrl/Cmd`，再把鼠标悬停在某个日期单元格上即可。

### 日历可以移动（也可以固定）到任意位置

虽然日历默认出现在右侧边栏，但并不必须放在那里。你可以拖到左侧边栏，或（如果屏幕空间够）拖到主内容区。如果移出边栏，还可以将该视图固定（pin），非常适合更高级的分栏布局。

![how-to-pin](./images/how-to-pin.png)

### 在新分栏中打开日记

在日历中按住 `Ctrl/Command` 再点击某一天，会在新分栏打开对应日记。适合连续打开多天笔记（尤其是启用了 **Sliding Panes** 插件时）。

### 在日历中定位当前打开笔记

如果你打开了其他月份的笔记，可能希望在日历中快速定位它。可在命令面板运行 `Calendar: Reveal open note`。

### 为周末添加自定义样式

如果你想让周末和工作日更容易区分，可以将 `var(--color-background-weekend)` 设为你想要的颜色。

![how-to-weekend](./images/how-to-weekend.png)

### Weekly Notes（已弃用）

#### 周记功能已迁移

周记功能已拆分为[独立插件](https://github.com/liamcain/obsidian-periodic-notes/)。未来该功能会从 Calendar 插件中移除；如果你正在使用周记，建议尽快迁移。无需担心，行为基本一致，且仍可与日历视图集成。

这次拆分受到 [One Thing Well](https://en.wikipedia.org/wiki/Unix_philosophy) 理念启发。插件应尽量模块化。有些用户只需要周记，不需要日历视图；反之亦然。

如果你当前在 Calendar 插件中使用周记，新版 Periodic Notes 插件会自动迁移你的设置。

### 用法

你可以通过两种方式打开**周记（weekly notes）**：在命令面板搜索 `Calendar: open weekly note`，或直接点击周数。周记可在 Calendar 设置中配置，共 3 项：

- **Folder：**周记存放目录。可以与日记相同，也可以不同。默认放在 vault 根目录。
- **Template：**周记模板。周记支持的模板标签与日记略有不同。支持列表见[周记模板标签](#模板标签)。

> 注意：这里的路径不会自动补全，需要你输入完整路径。

- **Format：**周记文件名的日期格式。默认值为 `"gggg-[W]ww`。如果在周格式中使用 `DD`，它表示该周第一天（周日或周一，取决于你的设置）。

#### 模板标签

| Tag                                                                                     | Description                                                                                                                                                                                                    |
| --------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `sunday`, `monday`, `tuesday`, `wednesday`, `thursday`, `friday`, `saturday`, `sunday` | 因为周记标签面向一周中的主要日期，你可以这样引用某一天：`{{sunday:gggg-MM-DD}}`，自动插入该日日期。注意：必须显式指定日期格式。                                                                                |
| `title`                                                                                 | 与日记中的 `{{title}}` 含义一致，会插入笔记标题。                                                                                                                                                                |
| `date`, `time`                                                                          | 与日记中的 `{{date}}`、`{{time}}` 含义一致，会插入该周第一天的日期和时间。可用于生成标题（例如 `# # {{date:gggg [Week] ww}}`）。                                                                                 |

## 实际演示

- [Nick Milo 的插件演示 walkthrough](https://www.youtube.com/watch?v=X61wRmfZU8Y&t=1099s)
- [Santi Younger 演示 Calendar + Periodic Notes 的每周复盘用法](https://www.youtube.com/watch?v=T9y8JABS9_Q)
- [Filipe Donadio 使用日历规划一天](https://www.youtube.com/watch?v=hxf3_dXIcqc)

## 致谢 🙏

如果你喜欢这个插件并愿意请我喝杯咖啡，欢迎支持！

[<img src="https://cdn.buymeacoffee.com/buttons/v2/default-violet.png" alt="BuyMeACoffee" width="100">](https://www.buymeacoffee.com/liamcain)

喜欢我的工作并希望看到更多类似内容？也可以赞助我。

[![GitHub Sponsors](https://img.shields.io/github/sponsors/liamcain?style=social)](https://github.com/sponsors/liamcain)
